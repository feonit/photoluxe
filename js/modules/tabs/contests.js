/**
 * Вкладка Contests
 *
 * */

define([], function(){

  /**
   * Pre-boot methods
   * @private
   * */

    return new function(){

        /**
         * Public Api
         * @public
         * */


        /**
         * Initialize tab (core API)
         * */

         this.init = function(){

            /**
             * Initialisation when DOM is ready
             * */

            /**
             * Bind events when DOM is ready
             * */
        };

        /**
         * Clear events, memory (core API)
         * */
        this.destroy = function(){

        };
    };
});
